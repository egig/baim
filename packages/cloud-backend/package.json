{
  "name": "sabi-cloud",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate": "wrangler d1 execute sabi-db --file=migrations/001_init.sql",
    "migrate:credits": "wrangler d1 execute sabi-db --file=migrations/002_credits.sql",
    "mint-key": "tsx scripts/mint-key.ts"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "tsx": "^4",
    "typescript": "^5"
  }
}
